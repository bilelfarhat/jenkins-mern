pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Installez les dépendances Node.js
                sh 'usr/bin/npm install'
            }
        }
        // Autres étapes de votre pipeline...
    }
}
