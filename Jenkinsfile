pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Installez les dépendances Node.js
                sh 'node --version'
            }
        }
        // Other stages of your pipeline...
    }
}
