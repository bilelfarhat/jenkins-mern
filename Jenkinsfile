pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               
                sh 'npm install'
            }
        }
        // Other stages of your pipeline...
    }
}
